# Live Demo

Click **Generate PDF** to render a multi-page PDF entirely in your browser.

The document demonstrates: linear gradient backgrounds, `::before`/`::after` pseudo-elements, RTL Arabic text, a repeat-table-header spanning multiple pages, custom Roboto font, page header/footer with page numbers, and `page-break="avoid"`.

<LiveDemo />
